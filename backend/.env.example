DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change_me
DB_NAME=inventario_fichas_wifi
JWT_SECRET=replace_with_a_strong_random_secret
PORT=3001
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX=500
LOGIN_RATE_LIMIT_MAX=20
LOGIN_RATE_LIMIT_WINDOW_MINUTES=15# Backend environment configuration
# Copy this file to .env and adjust values for your setup

# Server
PORT=3000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=inventario_fichas_wifi

# Auth
# Use a long random string; keep it secret and never commit it
JWT_SECRET=please-change-this-to-a-long-random-string

# CORS
# No need to set origins here; server has a permissive dev whitelist (localhost/127.0.0.1)

# Geocoding (Nominatim) bias and headers
# Limit to country codes (comma-separated). Example: mx
GEOCODER_COUNTRYCODES=mx
# Bias results to a bounding box: minLon,minLat,maxLon,maxLat
# Example below covers most of Oaxaca state
GEOCODER_VIEWBOX=-98.6,15.7,-94.0,18.9
# Custom User-Agent per Nominatim usage policy
GEOCODER_USER_AGENT=InventarioFichas/1.0 (contact: your-email@example.com)
